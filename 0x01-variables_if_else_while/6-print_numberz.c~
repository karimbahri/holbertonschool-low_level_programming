#include <stdio.h>
/**
*main - display numbers using putchar
*Return: 0
*/
int main(void)
{
	int number = 0;

	for (number = 0 ; number < 10 ; number++)
		putchar(number);
	putchar('\n');
	return (0);
}
